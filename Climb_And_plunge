local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer

-- Remote Events Path
local remoteFolder = ReplicatedStorage:WaitForChild("Remote"):WaitForChild("Event")
local addCoinsOrWinsEvent = remoteFolder:WaitForChild("Race"):WaitForChild("AddCoinsOrWins")
local getRewardEvent = remoteFolder:WaitForChild("Online"):WaitForChild("[C-S]IWannaGetThisReward")
local getSpinRewardEvent = remoteFolder:WaitForChild("Spin"):WaitForChild("[C-S]GetSpinReward")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RaceRewardGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 100
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = player:WaitForChild("PlayerGui")

-- Open / Close Button (Bulat & Draggable)
local OpenBtn = Instance.new("TextButton")
OpenBtn.Name = "OpenBtn"
OpenBtn.Size = UDim2.new(0, 45, 0, 45)
OpenBtn.Position = UDim2.new(0, 20, 0.3, 0)
OpenBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
OpenBtn.BackgroundTransparency = 0
OpenBtn.Text = "MENU"
OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenBtn.TextSize = 10
OpenBtn.Font = Enum.Font.SourceSansBold
OpenBtn.Active = true
OpenBtn.Parent = ScreenGui

local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(1, 0)
OpenCorner.Parent = OpenBtn

local OpenStroke = Instance.new("UIStroke")
OpenStroke.Color = Color3.fromRGB(255, 0, 50)
OpenStroke.Thickness = 2
OpenStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
OpenStroke.Parent = OpenBtn

-- Main Frame (Kotak Solid, Border Merah Neon)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 160, 0, 175)
MainFrame.Position = UDim2.new(0, 75, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BackgroundTransparency = 0
MainFrame.Visible = true
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(255, 0, 50)
MainStroke.Thickness = 2
MainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
MainStroke.Parent = MainFrame

-- Button Inf Wins
local WinsBtn = Instance.new("TextButton")
WinsBtn.Name = "WinsBtn"
WinsBtn.Size = UDim2.new(1, -12, 0, 34)
WinsBtn.Position = UDim2.new(0, 6, 0, 8)
WinsBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
WinsBtn.BackgroundTransparency = 0
WinsBtn.Text = "INF WINS"
WinsBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
WinsBtn.TextSize = 11
WinsBtn.Font = Enum.Font.SourceSansBold
WinsBtn.Parent = MainFrame

-- Button Inf Money
local MoneyBtn = Instance.new("TextButton")
MoneyBtn.Name = "MoneyBtn"
MoneyBtn.Size = UDim2.new(1, -12, 0, 34)
MoneyBtn.Position = UDim2.new(0, 6, 0, 48)
MoneyBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MoneyBtn.BackgroundTransparency = 0
MoneyBtn.Text = "INF MONEY"
MoneyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MoneyBtn.TextSize = 11
MoneyBtn.Font = Enum.Font.SourceSansBold
MoneyBtn.Parent = MainFrame

-- Button Get All Reward
local RewardBtn = Instance.new("TextButton")
RewardBtn.Name = "RewardBtn"
RewardBtn.Size = UDim2.new(1, -12, 0, 34)
RewardBtn.Position = UDim2.new(0, 6, 0, 88)
RewardBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
RewardBtn.BackgroundTransparency = 0
RewardBtn.Text = "GET ALL REWARD"
RewardBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
RewardBtn.TextSize = 11
RewardBtn.Font = Enum.Font.SourceSansBold
RewardBtn.Parent = MainFrame

-- Button Get 250% OP PET
local PetBtn = Instance.new("TextButton")
PetBtn.Name = "PetBtn"
PetBtn.Size = UDim2.new(1, -12, 0, 34)
PetBtn.Position = UDim2.new(0, 6, 0, 128)
PetBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
PetBtn.BackgroundTransparency = 0
PetBtn.Text = "GET 250% OP PET"
PetBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
PetBtn.TextSize = 11
PetBtn.Font = Enum.Font.SourceSansBold
PetBtn.Parent = MainFrame

-- Draggable Engine
local function makeDraggable(guiObject)
	local dragging = false
	local dragInput, dragStart, startPos

	guiObject.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = guiObject.Position

			input.Changed:Connect(function(inputState)
				if inputState.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	guiObject.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			guiObject.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end

makeDraggable(MainFrame)
makeDraggable(OpenBtn)

-- Toggle Menu Visible
OpenBtn.MouseButton1Click:Connect(function()
	MainFrame.Visible = not MainFrame.Visible
end)

-- Inf Wins Click
WinsBtn.MouseButton1Click:Connect(function()
	addCoinsOrWinsEvent:FireServer(math.huge, "Wins")
end)

-- Inf Money Click
MoneyBtn.MouseButton1Click:Connect(function()
	addCoinsOrWinsEvent:FireServer(math.huge, "Coins")
end)

-- Get All Reward Click (1 to 12)
RewardBtn.MouseButton1Click:Connect(function()
	for i = 1, 12 do
		getRewardEvent:FireServer(i)
	end
end)

-- Get 250% OP PET Click
PetBtn.MouseButton1Click:Connect(function()
	getSpinRewardEvent:FireServer(300006)
end)
